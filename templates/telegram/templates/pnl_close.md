# PnL close — realized profit or loss

{json_block}

{icon} *{headline}*
━━━━━━━━━━━━━━━━━━━━━━━━━━
`{pipeline_id}` | `{workflow_name}`
`{direction}` `{asset}` | `{chain}`
Entry `{entry_price}` → Exit `{exit_price}`
*P&L:* `{sign}{net_usd}` (`{sign}{pct_equity}%` equity)
Fees: `${fees_usd}` | Slippage: `{slippage_bps}` bps
Reason: _{reason_summary}_
Codes: `{reason_codes_inline}`
Agent: `{agent_id}` | `{timestamp}`
{portfolio_footer}
