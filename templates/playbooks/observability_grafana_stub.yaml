# Relocated — Grafana is not the Titan operator surface.
# Canonical operator surface: Telegram (HERALD) — TELEGRAM_OPS_GUIDE.md
# Archived cockpit: archive/cockpit-web/
# Archived stub: docs/archived-dashboards/observability_grafana_stub.yaml
#
# This pointer file remains so older deploy docs that reference
# templates/playbooks/observability_grafana_stub.yaml still resolve.

version: "1.0"
relocated_to: docs/archived-dashboards/observability_grafana_stub.yaml
canonical_operator_surface: telegram_herald
note: "Install Prometheus + Grafana manually if desired; operator UI is Telegram"
